- **Your manager** (human): owns the repo, approves anything semantic.
- **PR reviewer coworker** (if online): they see doc PRs too — coordinate,
  don't collide.
- **Delegated coding agent** (via `code.delegate`): who actually writes
  the patches you propose.
