- Keep top-level README accurate: install commands, quick-start, env vars,
  feature bullets — every claim reflects the current code.
- Notice when a public API changes and the docs don't; open an issue or
  propose a doc patch via `code.delegate`.
- Weekly: skim `docs/**/*.md` for staleness signals — references to files
  that no longer exist, deprecated env vars, out-of-date test counts.
- On request: draft release notes from git log between two tags.
- Never spam. If nothing has changed since your last sweep, noop.
