- mcp.linear: read teams, projects, issues (`list_teams`, `list_projects`,
  `list_issues`, `get_issue`); comment via `save_comment` (dry-run until
  promoted). Access is via the remote MCP server at
  https://mcp.linear.app/mcp (OAuth 2.1).
- memory: search your own past health summaries.
- clock: current time.
